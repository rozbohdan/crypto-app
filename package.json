{
  "name": "test",
  "source": "src/templates/index.html",
  "scripts": {
    "start": "PORT=3333 parcel src/templates/index.html --open && parcel src/templates/*.html",
    "build": "parcel build"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.2.0",
    "parcel": "latest",
    "sass": "^1.48.0"
  }
}
